# dom-simulator
DOM concept
# DOM Simulator Demo

A proof-of-concept Flutter app for a DOM ladder, simulated orders, and trade logic.

## 🚀 Quick Start

1. Fork this repo on GitHub.  
2. Open it in **GitHub Codespaces** (or GitPod).  
3. In the terminal, run:
   ```bash
   flutter pub get
   flutter run -d chrome
